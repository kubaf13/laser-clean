import { Container } from '@atoms/Loader/Loader.styled';

import type { Loader as LoaderType } from './Loader.types';

export const Loader: LoaderType = () => (
  <Container>
    <span />
    <span />
    <span />
  </Container>
);
